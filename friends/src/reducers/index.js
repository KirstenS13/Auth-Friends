export const initialState = {
    friends: [],
    loginToken: "",
    isLoading: false
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        case "LOGIN":
            
        default:
            return state;
    }
}